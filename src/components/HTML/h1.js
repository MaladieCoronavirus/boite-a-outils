import styled from '@emotion/styled';

import { colors } from 'Style/colors';

const H1 = styled.h1`
  margin-bottom: 1rem;
  font-size: 2rem;
  font-weight: 700;
  color: ${colors.primary};
`

export default H1;
