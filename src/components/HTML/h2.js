import styled from '@emotion/styled';

import { colors } from 'Style/colors';

const H2 = styled.h2`
  margin-bottom: 1rem;
  font-size: 1.75rem;
  line-height: 1.21428;
  font-weight: 500;
  color: ${colors.accent};
  text-align: center;
`

export default H2;
