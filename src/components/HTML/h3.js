import styled from '@emotion/styled';

import { colors } from 'Style/colors';

const H3 = styled.h3`
  margin-bottom: 0.5rem;
  font-size: 1.75rem;
  line-height: 1.21428;
  font-weight: 500;
  color: ${colors.accent};
  text-align: center;
`

export default H3;
